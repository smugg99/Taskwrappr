# test.tw

# here is a comment
# as you can see, comments start with a hash
# and continue until the end of the line
# they are ignored by the interpreter

#if(true)
#{
#	print("hello world")
#}

print(print(print(15.2, 12, "hello world", true, false, false)), 44, -2137)

print("start")

if(xor(true, false, and(true, false, true)))
{
	print("xor true")
}
else()
{
	print("xor false")
}

if(true)
{
	print("true")
	if(true)
	{
		print("nested true")
		if (false)
		{
			print("nested false")
		}

		if(true)
		else()
		{
			print("nested elses")
			else()
		}
	}
}
else()
{
	print("elses")
	print("more elses")
	if(false)
	{
		print("always false")
	}
	else()
	{
		print("always true")
		if (true) {
			print("also true")
		}
	}
}

print("done")